// Finding a value's index in an array with .indexOf()

const colors = ['red', 'blue', 'green', 'yellow', 'brown', 'purple'];

const index1 = colors.indexOf('yellow', 0);
const index2 = colors.indexOf('orange', 0);

console.log(`\nFirst index: ${index1}`);
console.log(`Second index: ${index2}\n`);
