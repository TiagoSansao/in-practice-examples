// Finding the index of word's first letter giving the start index.

const text = 'this coffee is smelling good.';

const indexOfCoffee = text.indexOf('coffee', 6);

console.log(`\nCoffe's index: ${indexOfCoffee}\n`);
